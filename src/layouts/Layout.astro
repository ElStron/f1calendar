---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<div class="layoutContainer">
			<Header/>
			<slot />
			<Footer/>
		</div>
	</body>
</html>
<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
		--background-gradient:background-image: linear-gradient(to right, #4facfe 0%, #00f2fe 100%);
		--background:#13151a;
		--color:white;
	}
	* {
    	box-sizing: border-box;
	}

	body {
		color: var(--color);
		background: var(--background);
		font-family: system-ui, sans-serif;
	}

	body::after {
		content: "";
		width: 230px;
		height: 400px;
		background-color: var(--background-gradient);
	}
	a, p, a:visited {
		color: var(--color);
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
	main {
		margin: 24px;
	}

	.layoutContainer {
		display: grid;
		margin: 0 auto;
		max-width: 1200px;
		height: 100vh;
		grid-template-rows: auto 1fr 45px;
		grid-gap: 25px;
	}


	ul, li, h1,h2, h3, a, p, span {
		margin: 0;
		padding: 0;
		text-decoration: none;
		list-style: none;
	}

	.container {
    	display: grid;
    	grid-template-columns: 1fr 374px;
		grid-gap:24px;
	}
	@media (width < 700px)  {
		.container {
			grid-template-columns: 1fr ;
			grid-gap:40px;
		}
	}
</style>
